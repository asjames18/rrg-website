---
/**
 * Redirect from old admin-dashboard to unified admin portal
 * This handles any old bookmarks or links
 */
export const prerender = false;

// Redirect to the unified admin portal
return Astro.redirect('/admin');
---